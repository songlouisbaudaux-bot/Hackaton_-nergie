{
  "name": "prometheus-protocol-v2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "simulate:pacing": "node scripts/simulate-progression.mjs",
    "audit:assets": "node scripts/audit-assets.mjs",
    "check:prod": "npm run build && npm run simulate:pacing && npm run audit:assets",
    "ios:install": "npm --prefix ../apps/ios install",
    "ios:sync": "npm run build && npm --prefix ../apps/ios run sync",
    "ios:open": "npm --prefix ../apps/ios run open",
    "balance:auto": "node scripts/auto-improve-balance.mjs",
    "balance:loop": "node scripts/auto-improve-balance.mjs --loop --apply"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "playwright-core": "^1.61.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
